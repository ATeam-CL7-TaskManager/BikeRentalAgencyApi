using System;
using System.ComponentModel.DataAnnotations;

namespace BikeRentalAgencyUI.Models
{
    //public class ValidateDateRange : ValidationAttribute
    //{
    //    protected override ValidationResult IsValid(object value)
    //    {
    //        // your validation logic
    //        if (Convert.ToDateTime(value) >= DateTime.Now && Convert.ToDateTime(value) <= (DateTime.Now.AddDays(30)))
    //        {
    //            return ValidationResult.Success;
    //        }
    //        else
    //        {
    //            return new ValidationResult("All Reservation dates must be between now and 30 days from today.");
    //        }
    //    }
    //}
}